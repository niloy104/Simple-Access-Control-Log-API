version: "3.9"

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./access_control/system_events.log:/app/access_control/system_events.log
    environment:
      - PYTHONUNBUFFERED=1
